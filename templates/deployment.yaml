{{- define "base-template.deployment" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.fullnameOverride }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{ .Values.fullnameOverride | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{ .Values.fullnameOverride | nindent 6 }}
  template:
    metadata:
      labels:
        {{ .Values.fullnameOverride | nindent 8 }}
    spec:
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
        - name: {{ .Values.fullnameOverride }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          envFrom:
            {{- if .Values.env.useConfigMap }}
            - configMapRef:
                name: {{ .Values.fullnameOverride }}-config
            {{- end }}
            {{- if .Values.env.useSecret }}
            - secretRef:
                name: {{ .Values.fullnameOverride }}-secret
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
{{- end }}